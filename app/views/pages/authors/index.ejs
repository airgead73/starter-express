<h2>
  <%= typeof heading !='undefined' ? heading : 'heading' %>
</h2>

<ul>
  <% authors.forEach(author=> { %>

    <li>
      <a href="/authors/<%= author._id %>">
        <%= author.listname %>
      </a>
    </li>

    <% }) %>
</ul>